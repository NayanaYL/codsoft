# Simple Rule-Based Chatbot

def chatbot_response(user_input):
    """
    Function to generate a response based on user input using simple rule-based matching.
    """
    # Convert the input to lowercase for case-insensitive matching
    user_input = user_input.lower()
    
    # Define predefined responses based on user input
    if 'hello' in user_input or 'hi' in user_input:
        return "Hello! How can I help you today?"
    elif 'how are you' in user_input:
        return "I'm just a bot, but I'm here to help you!"
    elif 'name' in user_input:
        return "I am a simple rule-based chatbot."
    elif 'help' in user_input:
        return "Sure! What do you need help with?"
    elif 'bye' in user_input or 'goodbye' in user_input:
        return "Goodbye! Have a great day!"
    else:
        return "I'm sorry, I don't understand that. Can you please rephrase?"

def main():
    """
    Main function to run the chatbot in a loop for continuous interaction.
    """
    print("Welcome to the simple rule-based chatbot!")
    print("Type 'bye' or 'goodbye' to exit the chat.")
    
    while True:
        user_input = input("You: ")
        if user_input.lower() in ['bye', 'goodbye']:
            print("Chatbot: " + chatbot_response(user_input))
            break


        else:
            print("Chatbot: " + chatbot_response(user_input))

# Run the chatbot
if __name__ == "__main__":
    main()

